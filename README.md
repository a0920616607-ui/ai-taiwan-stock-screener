# AI台灣個股智慧選股 V10.7

- 上市、上櫃與混合搜尋結果統一依 AI 分數由高至低排列。
- 已掃描批次合併後重新全域排序，避免第三頁分數高於第一頁。
- 排序選項預設並固定為「AI 分數高→低」，切換其他排序仍可使用。

# AI 台灣個股智慧選股 V10.7

真正修正：
- 首頁、標題、manifest 與 health API 全部更新為 V10.7。
- 上市＋上櫃由伺服器分別取得兩個市場，每頁固定平衡組合，不再沿用單一市場批次。
- TWSE 三大法人 API 同時支援 root fields/data 與 tables[0].fields/data 格式。
- 法人快取改用 V10.7 新檔，避免舊版「待更新」污染。
- 本機股票名單快取改為 v105 key。
