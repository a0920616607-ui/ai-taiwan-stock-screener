services:
  - type: web
    name: ai-taiwan-stock-screener-v7-1
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    healthCheckPath: /api/health
    autoDeploy: true
