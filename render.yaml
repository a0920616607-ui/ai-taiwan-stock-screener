services:
  - type: web
    name: ai-taiwan-stock-screener-v13-2
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 300 --workers 1 --threads 4
    healthCheckPath: /api/health
    autoDeploy: true
