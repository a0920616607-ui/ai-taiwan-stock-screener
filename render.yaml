services:
  - type: web
    name: ai-taiwan-stock-screener-v6-5
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    healthCheckPath: /api/health
    autoDeploy: true
